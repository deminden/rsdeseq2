name: Parity

on:
  workflow_dispatch:

jobs:
  parity-placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "DESeq2 parity workflow will run after references are versioned."
