# Python

Python bindings are future work. The initial repository contains only a
placeholder crate directory so the workspace shape can grow consistently later.

The expected package name is `rsdeseq2`.
